Flux-tube candidate skeleton

Define ψ_R supported on a tube of length R and fixed cross section
Normalize L(ψ_R)=1 by scaling amplitude
Compute E_R = <ψ_R, H ψ_R>
If E_R -> 0 as R -> infinity then a violating sequence exists
Record which clause fails if computation uses assumptions
