# Transfer Operator Framework

Let T_L denote the finite-volume transfer operator constructed
from blocked Yang–Mills configurations on lattice size L.

Define the normalized operator

S_L = T_L / ||T_L||.

We study the spectrum

λ₁ ≥ λ₂ ≥ ...

The finite-volume spectral gap is

gap(L) = λ₁ − λ₂.
