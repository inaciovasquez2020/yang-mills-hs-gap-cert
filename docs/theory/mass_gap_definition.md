# Mass Gap Definition

Let T_L be the finite-volume transfer operator
constructed from blocked Yang–Mills configurations.

Let eigenvalues be

λ₁ ≥ λ₂ ≥ ...

Define the spectral gap

gap(L) = λ₁ − λ₂.

If

lim inf_{L→∞} gap(L) ≥ m > 0

then the theory has a positive mass gap m.
