# L2a Linear Representation Target

## Status

Conditional.

## Current next object

\[
\mathbf{(L2a)}\quad
\phi(f)=\sum_{x\in\Lambda_n}\langle K_{n,f}(x),U_x\rangle.
\]

## Weakest sufficient formulation

For each \(n\) and each \(f\in\mathcal S(\mathbb R^4,\mathfrak g)\), there exists a finitely supported coefficient field
\[
K_{n,f}:\Lambda_n\to\mathfrak g
\]
such that
\[
\phi(f)=\sum_{x\in\Lambda_n}\langle K_{n,f}(x),U_x\rangle.
\]

## Linearity requirements

\[
K_{n,\alpha f+\beta g}(x)=\alpha K_{n,f}(x)+\beta K_{n,g}(x)
\qquad
\forall \alpha,\beta\in\mathbb R,\ \forall f,g,\ \forall x\in\Lambda_n.
\]

## Reduction lock

\[
\mathbf{(L2a)}+\mathbf{(L2b)}+\text{locality of }\Gamma_n
\Longrightarrow
\mathbf{(L2)}.
\]

## Next admissible micro-fix

\[
\boxed{\text{define }K_{n,f}(x)\text{ explicitly from the lattice smearing rule}}
\]
