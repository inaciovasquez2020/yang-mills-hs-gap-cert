# Finite Volume Gap Scaling Test

Let λ₁ ≥ λ₂ ≥ ... be eigenvalues of the transfer operator.

Define the spectral gap

gap(L) = λ₁ - λ₂

computed on lattice volume L.

Procedure

1 compute transfer operator
2 compute eigenvalues
3 record gap(L)

Stability criterion

gap(L) ≥ c > 0 independent of L
