# Strong Resolvent Convergence Schema

Let \(H_L\) be self-adjoint operators generated by closed forms \(q_L\).

If

1. \(q_L(f) \to q_\infty(f)\) for all \(f\) in a common dense domain.
2. \(q_L\) are uniformly lower bounded.

Then

\[
H_L \xrightarrow{s.r.} H_\infty.
\]
