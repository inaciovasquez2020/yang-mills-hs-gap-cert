# Interval Helly Reduction

Project block supports onto each coordinate axis.

Each projection yields an interval family.

Pairwise overlap implies global overlap in 1D.

Thus a coordinate center exists.

Combining all coordinates gives a vertex v with

supp(Φ(γ)) ⊂ B_R(v).
