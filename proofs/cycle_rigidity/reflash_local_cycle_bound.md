# REflash Local Cycle Bound

Assumption

diam(γ_i ∩ γ_j) ≤ R

and G is a lattice block-intersection graph.

Then every admissible cycle γ is contained inside some ball B_R(v).

Hence

Z1(G) = ⊕_v Z1(B_R(v))

and therefore

C_R(G) ≤ max_v dim Z1(B_R(v)).
