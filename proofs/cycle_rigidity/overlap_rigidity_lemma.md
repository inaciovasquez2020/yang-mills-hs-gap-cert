# Overlap Rigidity Lemma

Let G be a bounded-degree graph with degree ≤ Δ.

Assume all radius-R neighborhoods are FO_k^R-equivalent.

Let C_R(G) be the F2-rank of cycles whose pairwise overlaps lie inside
radius-R neighborhoods.

Then

C_R(G) ≤ T(k,Δ)

for a constant independent of |V(G)|.
