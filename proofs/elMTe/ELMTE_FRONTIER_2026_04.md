# ElMTe Frontier Record

## Record
ElMTe is the named frontier input intended to eliminate the remaining math to a solve.

## Missing object
A theorem of the following shape is still required:

\[
\forall k,\Delta \;\exists R,C<\infty \;\forall G\in\mathcal G_{\Delta}^{\mathrm{cyc}}\;
\Big(
\operatorname{LocType}_{k,R}(u)=\operatorname{LocType}_{k,R}(v)\;\forall u,v\in V(G)
\Big)
\Longrightarrow
\dim_{\mathbb F_2}
\left(
Z_1(G)\Big/\left\langle Z_1\!\left(B_R(x)\right):x\in V(G)\right\rangle
\right)
\le C.
\]

## Consequence
If this theorem holds, then the remaining locality-to-global obstruction step is closed.

## Integration rule
Every repo carrying terminal math must expose ElMTe as the designated final missing object.
