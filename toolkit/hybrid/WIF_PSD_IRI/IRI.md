# IRI — Infrared Irreducibility (New Input)

Statement:
  ∃ c,β>0, E0>0 such that ∀ E∈(0,E0],
  || P_{≤E} H_int(Λ) P_{≤E} || ≥ c E^β.

Role:
  Together with WIF (IR flow suppression) and PSD (polynomial IR density), IRI forces an IR lift and yields a uniform gap.
